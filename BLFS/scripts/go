
cd "$SRC"

wget https://go.dev/dl/go1.25.10.linux-amd64.tar.gz

tar -C /usr/local/ -xf go1.25.10.linux-amd64.tar.gz

cat >> /etc/profile << "EOF"
export PATH=$PATH:/usr/local/go/bin
EOF

source /etc/profile
